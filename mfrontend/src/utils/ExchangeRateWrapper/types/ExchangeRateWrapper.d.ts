//
import { IExchangeRate } from '../../ExchangeRate/types/exchangeRate';

export namespace IExchangeRateWrapper {
  export interface IExchangeRateWrapperOptions {
    exchangeRatesSteamList?: IExchangeRate.ExchangeRatesList;
  }
}
